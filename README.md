# Nuget setup

1. Create a folder called C:\tempnuget
2. Drop the nupkg file into the folder in #1


# SQL Server setup

1. Connect to sql and setup the database using setup.sql
2. In the App.Config, modify the connection string to point to the SQL server. 
3. Run the application.

